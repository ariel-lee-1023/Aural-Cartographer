# Round-two root-ledger snapshot

These files preserve the prior root ledger byte-for-byte before the third-round fold-in. Relative links in historical Markdown retain their original meaning relative to the root fidelity-ledger directory; they are not rewritten. Historical validation includes the pre-existing display-name/frontmatter slug error. Current validation is at the root of fidelity-ledger. The display name was preserved while the runtime slug was repaired in round three.
